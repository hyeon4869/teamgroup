server:
  port: 8081

#database 연동 설정.
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_beenie6?serverTimezone=Asia/Seoul&characterEncoding-UTF-8

    #MySQL에서 생성한 사용자 계정 정보 기입.
    username: root
    password: 12341234
  thymeleaf:
    cache: false

  #spring data jpa 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update

#test8

